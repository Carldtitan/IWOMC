{
  "name": "@environment-reconciler/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "clean": "rimraf dist",
    "db:migrate": "node --env-file=../../.env scripts/migrate-database.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@environment-reconciler/adapters": "workspace:*",
    "@environment-reconciler/contracts": "workspace:*",
    "@environment-reconciler/integrations": "workspace:*",
    "@environment-reconciler/reconciler": "workspace:*",
    "hono": "^4.12.32",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "rimraf": "^6.0.1",
    "typescript": "6.0.3"
  }
}
