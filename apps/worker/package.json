{
  "name": "@environment-REDACTED/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@environment-REDACTED/contracts": "workspace:*",
    "hono": "^4.12.32"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "6.0.3"
  }
}
