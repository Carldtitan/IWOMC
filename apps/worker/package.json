{
  "name": "@environment-REDACTED/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@environment-REDACTED/adapters": "workspace:*",
    "@environment-REDACTED/contracts": "workspace:*",
    "@environment-REDACTED/integrations": "workspace:*",
    "@environment-REDACTED/REDACTED": "workspace:*",
    "hono": "^4.12.32",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "rimraf": "^6.0.1",
    "typescript": "6.0.3"
  }
}
