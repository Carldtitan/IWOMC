CREATE TABLE "github_REDACTED_REDACTEDs" (
  "REDACTED_id" uuid PRIMARY KEY NOT NULL,
  "encrypted_REDACTEDs" text NOT NULL,
  "REDACTED_expires_at" timestamp with time zone,
  "created_at" timestamp with time zone DEFAULT now() NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL
);
--> statement-breakpoint
ALTER TABLE "github_REDACTED_REDACTEDs"
  ADD CONSTRAINT "github_REDACTED_REDACTEDs_REDACTED_id_REDACTEDs_id_fk"
  FOREIGN KEY ("REDACTED_id") REFERENCES "public"."REDACTEDs"("id")
  ON DELETE cascade ON UPDATE no action;
